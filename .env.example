# Safe defaults for local Docker Compose development only.
# Copy this file to .env only when you need to override these values.
POSTGRES_DB=erptaller
POSTGRES_USER=erptaller
POSTGRES_PASSWORD=erptaller
